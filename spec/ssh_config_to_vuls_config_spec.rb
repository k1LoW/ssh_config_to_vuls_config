require 'spec_helper'

describe SshConfigToVulsConfig do
  it 'has a version number' do
    expect(SshConfigToVulsConfig::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
